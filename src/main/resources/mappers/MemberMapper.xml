<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace = "com.example.demo.dao.MemberDao">
	<insert id ="saveMember" parameterType="Member">
		INSERT INTO member(
			id,
			password,
			name,
			gender,
			birth,
			mail,
			phone,
			address,
			reg,
		    grade
		)
		VALUES(
			#{id},
			#{password},
			#{name},
			#{gender},
			#{birth},
			#{mail},
			#{phone},
			#{address},
			sysdate,
			(SELECT grade.name FROM grade WHERE grade.code = 2)
		)
	</insert>

	<select id="getGrade" parameterType="String" resultType="String">
		SELECT member.grade
		FROM member
		WHERE member.id = #{id}
		AND member.password = #{pw}
	</select>

	<select id="findAllMember" resultType="Member">
		SELECT *
		FROM member
	</select>

</mapper>